[
    {
        "algorithm": "The algorithm computes eight stable features: global intensity mean and std, central-region intensity mean and std, horizontal and vertical gradient magnitudes, left-right mirror symmetry difference, and a high-intensity pixel proportion.",
        "code": "import numpy as np\n\ndef extract_features(image: np.ndarray) -> np.ndarray:\n    image = np.asarray(image, dtype=float)\n    h, w = image.shape\n\n    centre = image[\n        int(0.20 * h):int(0.85 * h),\n        int(0.20 * w):int(0.80 * w)\n    ]\n\n    dx = np.diff(image, axis=1)\n    dy = np.diff(image, axis=0)\n\n    half = w // 2\n    left = image[:, :half]\n    right = np.fliplr(image[:, w-half:])\n\n    threshold = np.mean(image) + np.std(image)\n\n    return np.array(\n        [\n            np.mean(image),\n            np.std(image),\n            np.mean(centre),\n            np.std(centre),\n            np.mean(np.abs(dx)),\n            np.mean(np.abs(dy)),\n            np.mean(np.abs(left - right)),\n            np.mean(image > threshold),\n        ],\n        dtype=float,\n    )",
        "objective": 0.05993,
        "other_inf": null
    },
    {
        "algorithm": "The new algorithm uses a larger central crop, additional diagonal gradients, a vertical symmetry measure, and a lower intensity threshold to emphasize texture and contrast differences.",
        "code": "import numpy as np\n\ndef extract_features(image: np.ndarray) -> np.ndarray:\n    image = np.asarray(image, dtype=float)\n    h, w = image.shape\n\n    # Larger central region (10% to 90% of both dimensions)\n    centre = image[\n        int(0.10 * h):int(0.90 * h),\n        int(0.10 * w):int(0.90 * w)\n    ]\n\n    # Horizontal, vertical, and diagonal gradients\n    dx = np.diff(image, axis=1)\n    dy = np.diff(image, axis=0)\n    dxy = np.diff(np.diff(image, axis=0), axis=1)  # diagonal via second diff\n\n    # Vertical symmetry difference (top vs bottom flipped)\n    half_h = h // 2\n    top = image[:half_h, :]\n    bottom = np.flipud(image[h-half_h:, :])\n\n    # Lower threshold: mean + 0.5 * std\n    threshold = np.mean(image) + 0.5 * np.std(image)\n\n    return np.array(\n        [\n            np.mean(image),\n            np.std(image),\n            np.mean(centre),\n            np.std(centre),\n            np.mean(np.abs(dx)) + np.mean(np.abs(dy)),\n            np.mean(np.abs(dxy)),\n            np.mean(np.abs(top - bottom)),\n            np.mean(image > threshold),\n        ],\n        dtype=float,\n    )",
        "objective": 0.07116,
        "other_inf": null
    },
    {
        "algorithm": "The new algorithm uses radial distance-weighted intensity moments, angular gradient projections, and quadrant-specific contrast ratios to encode spatial distribution patterns differently from the provided codes.",
        "code": "import numpy as np\n\ndef extract_features(image: np.ndarray) -> np.ndarray:\n    image = np.asarray(image, dtype=float)\n    h, w = image.shape\n    \n    # Radial distance-weighted intensity moments (polar coordinates)\n    yy, xx = np.indices(image.shape)\n    cy, cx = h / 2, w / 2\n    r = np.sqrt((yy - cy)**2 + (xx - cx)**2)\n    r_norm = r / r.max()\n    \n    weight_mean = np.average(image, weights=1 + r_norm)\n    weight_std = np.sqrt(np.average((image - weight_mean)**2, weights=1 + r_norm))\n    \n    # Angular gradient projections (edge direction energy)\n    gy, gx = np.gradient(image)\n    angle = np.arctan2(gy, gx)\n    mag = np.sqrt(gx**2 + gy**2)\n    \n    horizontal_energy = np.mean(mag * (np.cos(angle)**2))\n    vertical_energy = np.mean(mag * (np.sin(angle)**2))\n    \n    # Quadrant contrast ratios (local extremes)\n    qh, qw = h//2, w//2\n    quad_means = np.array([\n        image[:qh, :qw].mean(),\n        image[:qh, qw:].mean(),\n        image[qh:, :qw].mean(),\n        image[qh:, qw:].mean()\n    ])\n    quadrant_range = quad_means.max() - quad_means.min()\n    quadrant_asym = (quad_means[0] - quad_means[1] - quad_means[2] + quad_means[3]) / (quadrant_range + 1e-12)\n    \n    # Distance-weighted central brightness (radial profile)\n    central_brightness = np.mean(image * (1 - r_norm))\n    \n    # Edge density in outer ring (perimeter texture)\n    ring_mask = (r_norm > 0.6).astype(float)\n    edge_density = np.mean(mag * ring_mask)\n    \n    # Intensity modulation frequency (zero-crossing rate)\n    center_line = image[:, w//2]\n    crossings = np.sum(np.diff(np.sign(center_line - center_line.mean())) != 0) / (h - 1)\n    \n    # Binarized centroid offset (mass distribution skew)\n    binary = (image > np.percentile(image, 75)).astype(float)\n    if binary.sum() > 0:\n        cy_bin = np.sum(binary * yy) / binary.sum()\n        cx_bin = np.sum(binary * xx) / binary.sum()\n        centroid_offset = np.sqrt((cy_bin - cy)**2 + (cx_bin - cx)**2) / np.sqrt(cx**2 + cy**2)\n    else:\n        centroid_offset = 0.0\n    \n    return np.array([\n        weight_mean,\n        weight_std,\n        horizontal_energy,\n        vertical_energy,\n        quadrant_asym,\n        central_brightness,\n        edge_density,\n        centroid_offset\n    ], dtype=float)",
        "objective": 0.09551,
        "other_inf": null
    },
    {
        "algorithm": "The algorithm computes eight features from local binary pattern uniformity, Laplacian variance, row/column projection kurtosis, quadrant intensity ratio, cross-shaped intensity profile, and binarized mass distribution, all using deterministic NumPy operations without relying on the previous code's structure.",
        "code": "import numpy as np\n\ndef extract_features(image: np.ndarray) -> np.ndarray:\n    image = np.asarray(image, dtype=float)\n    h, w = image.shape\n\n    # Local binary pattern uniformity (3x3 neighborhoods)\n    pad = np.pad(image, 1, mode='edge')\n    center = pad[1:-1, 1:-1]\n    neighbors = np.stack([\n        pad[:-2, :-2], pad[:-2, 1:-1], pad[:-2, 2:],\n        pad[1:-1, :-2], pad[1:-1, 2:],\n        pad[2:, :-2], pad[2:, 1:-1], pad[2:, 2:]\n    ])\n    lbp = (neighbors >= center).astype(float)\n    uniform = (np.sum(lbp, axis=0) != 4).mean()\n\n    # Laplacian variance (second derivative magnitude)\n    lap = np.diff(np.diff(pad, axis=0), axis=0)[:, 1:-1] + np.diff(np.diff(pad, axis=1), axis=1)[1:-1, :]\n    lap_var = np.var(lap)\n\n    # Row and column projection kurtosis\n    row_proj = image.mean(axis=1)\n    col_proj = image.mean(axis=0)\n    row_kurt = np.mean((row_proj - row_proj.mean())**4) / (row_proj.std()**4 + 1e-12)\n    col_kurt = np.mean((col_proj - col_proj.mean())**4) / (col_proj.std()**4 + 1e-12)\n\n    # Quadrant intensity ratio (top-left + bottom-right) / (top-right + bottom-left)\n    qh, qw = h//2, w//2\n    tl = image[:qh, :qw].mean()\n    tr = image[:qh, qw:].mean()\n    bl = image[qh:, :qw].mean()\n    br = image[qh:, qw:].mean()\n    quadrant_ratio = (tl + br) / (tr + bl + 1e-12)\n\n    # Cross-shaped intensity profile (center row and column deviation)\n    center_row = image[h//2, :].mean()\n    center_col = image[:, w//2].mean()\n    cross_profile = abs(center_row - center_col)\n\n    # Binarized mass distribution (variance of binary pixel coordinates)\n    binary = (image > np.median(image)).astype(float)\n    ys, xs = np.indices(image.shape)\n    mass_mean = binary.mean()\n    mass_var = (binary * ((xs - xs.mean())**2 + (ys - ys.mean())**2)).sum() / (binary.sum() + 1e-12)\n\n    return np.array([\n        uniform,\n        lap_var,\n        row_kurt,\n        col_kurt,\n        quadrant_ratio,\n        cross_profile,\n        mass_var,\n        mass_mean\n    ], dtype=float)",
        "objective": 0.09738,
        "other_inf": null
    }
]