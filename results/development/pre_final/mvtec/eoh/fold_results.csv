dataset,method,fold,seed,n_train,n_validation,n_features,feature_extraction_time,classifier_training_time,train_accuracy,train_balanced_accuracy,train_macro_f1,train_weighted_f1,validation_accuracy,validation_balanced_accuracy,validation_macro_f1,validation_weighted_f1,train_precision,train_recall,validation_precision,validation_recall,program
MVTEC,eoh,1,42,862,863,8,0.19990529981441796,0.05573250004090369,0.7517401392111369,0.5596866748091186,0.5437574198654531,0.6852714350903514,0.7450753186558516,0.5473584444172679,0.5227825702278257,0.6718583522805766,0.754601226993865,0.9777424483306836,0.7484811664641555,0.9793322734499205,"import numpy as np

def extract_features(image: np.ndarray) -> np.ndarray:
    image = np.asarray(image, dtype=float)
    h, w = image.shape

    top = image[:int(0.5*h), :]
    bottom = image[int(0.5*h):, :]

    centre = image[
        int(0.25*h):int(0.75*h),
        int(0.25*w):int(0.75*w)
    ]

    dx = np.diff(image, axis=1)
    dy = np.diff(image, axis=0)

    # diagonal gradients
    d1 = np.abs(np.diff(image, axis=0)[:, :-1])
    d2 = np.abs(np.diff(image, axis=0)[:, 1:])

    half_h = h // 2
    top_half = image[:half_h, :]
    bottom_half = np.flipud(image[h-half_h:, :])

    threshold = np.mean(image) - np.std(image)

    return np.array([
        np.mean(top),
        np.mean(bottom),
        np.mean(np.abs(centre - np.mean(centre))),
        np.mean(np.abs(dx)),
        np.mean(np.abs(dy)),
        np.mean(d1 - d2),
        np.mean(np.abs(top_half - bottom_half)),
        np.mean(image < threshold)
    ], dtype=float)"
MVTEC,eoh,2,43,863,862,8,0.1944987999740988,0.02257380005903542,0.7404403244495944,0.5441787941787942,0.5197742358599309,0.668770996531584,0.7494199535962877,0.5580968496898817,0.5421252987871454,0.6836575113809735,0.7472527472527473,0.972972972972973,0.7539975399753998,0.9745627980922098,"import numpy as np

def extract_features(image: np.ndarray) -> np.ndarray:
    image = np.asarray(image, dtype=float)
    h, w = image.shape

    top = image[:int(0.5*h), :]
    bottom = image[int(0.5*h):, :]

    centre = image[
        int(0.25*h):int(0.75*h),
        int(0.25*w):int(0.75*w)
    ]

    dx = np.diff(image, axis=1)
    dy = np.diff(image, axis=0)

    # diagonal gradients
    d1 = np.abs(np.diff(image, axis=0)[:, :-1])
    d2 = np.abs(np.diff(image, axis=0)[:, 1:])

    half_h = h // 2
    top_half = image[:half_h, :]
    bottom_half = np.flipud(image[h-half_h:, :])

    threshold = np.mean(image) - np.std(image)

    return np.array([
        np.mean(top),
        np.mean(bottom),
        np.mean(np.abs(centre - np.mean(centre))),
        np.mean(np.abs(dx)),
        np.mean(np.abs(dy)),
        np.mean(d1 - d2),
        np.mean(np.abs(top_half - bottom_half)),
        np.mean(image < threshold)
    ], dtype=float)"
