# Notebooks

`analysis/poster_visualisations.ipynb` is the maintained notebook for recreating
the final figures and supporting tables.

`archive/` preserves six exploratory notebooks from the development process.
They are historical records rather than the authoritative experiment interface;
use the modules under `src/experiments/` for reproducible runs.
