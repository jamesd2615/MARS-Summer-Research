# Vendored dependencies

`eoh/` contains the upstream Evolution of Heuristics implementation used by
the project. Its original licence and attribution files are retained inside
that directory.

Install the package from the repository root with:

```bash
python -m pip install -e vendor/eoh/eoh
```
